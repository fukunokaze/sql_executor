# sql_executor

A .NET console application to execute multiple SQL scripts against a SQL Server database.

This repository will contain a console application that can read and execute SQL scripts from a directory.